name: geno-research
version: 0.4.0
description: Research wiki, paper generation, repo docs

# No venv, runtime, or config defaults — all commands are pure markdown workflows.

commands:
  source: commands/
